import React from 'react'
import ReactDOM from 'react-dom/client'
import { App } from './App'


// Tópicos relevantes:
// Componentes e propriedades
// Estilizacoes globais CSS
// Variáveis no CSS
// Responsividade
// Iteracoes no jsx
// handleFunctions


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
